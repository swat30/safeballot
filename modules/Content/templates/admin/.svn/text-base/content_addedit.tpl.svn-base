{if $user->hasPerm('editcontent')}
<div class="notice"> Do not paste from Microsoft Word or other similar word processing programs. 
These programs <strong>will</strong> generate bad styling that will negatively affect the proper styling 
of your site.</div>

{$form->display()}
{/if}
